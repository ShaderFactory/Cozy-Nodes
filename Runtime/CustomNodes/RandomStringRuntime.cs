namespace ShaderFactory.CozyGraphToolkit.Runtime
{
    public class RandomStringRuntime : RuntimeCozyNode
    {

        /* public override object GetValue(string portName)
        {
            if (port.name == "Value")
            {
                for (int i = 0; i < 5; i++)
                {
                    stringList.Add((string) CozyHelper.GetValue(port));
                }
                return stringList[2];
            }
            return null;
        }*/
    }
}
